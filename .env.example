# Google OAuth Configuration Example
# Copy this file to .env and replace with your actual values

# iOS Client ID from Google Cloud Console
GOOGLE_IOS_CLIENT_ID=your-ios-client-id.apps.googleusercontent.com

# Android Client ID from Google Cloud Console
GOOGLE_ANDROID_CLIENT_ID=your-android-client-id.apps.googleusercontent.com

# Web Client ID from Google Cloud Console
GOOGLE_WEB_CLIENT_ID=your-web-client-id.apps.googleusercontent.com

# Redirect URIs
GOOGLE_MOBILE_REDIRECT_URI=mygallery://oauth
GOOGLE_WEB_REDIRECT_URI=http://localhost:19006/oauth

# Development configuration
# Set to true for mock authentication during development
# Set to false when using real Google OAuth
USE_MOCK_AUTH=true

# Optional: Add other auth service URLs if needed
# AUTH_BASE_URL=https://your-auth-server.com
# API_BASE_URL=https://your-api-server.com

# Instructions:
# 1. Go to Google Cloud Console (https://console.cloud.google.com/)
# 2. Create or select a project
# 3. Enable Google OAuth2 API
# 4. Create OAuth 2.0 Client IDs for iOS, Android, and Web
# 5. Replace the placeholder values above with your actual client IDs
# 6. Set USE_MOCK_AUTH=false when ready to use real authentication